import java.util.Scanner;
public class HanoiDemo {

	public static void main(String[] args) {
		Scanner kbd = new Scanner(System.in);
		int n;
		System.out.print("Enter a number of discs: ");
		n = kbd.nextInt();
		
		long startTime, endTime, executionTime;
		startTime = System.currentTimeMillis();
		moveDiscs(n, 1, 3, 2, 0);
		endTime = System.currentTimeMillis();
		executionTime = endTime - startTime;
		
		System.out.println("Execution Time: "+ executionTime + " ms");
	}
	
	public static void moveDiscs(int n, int from, int to, int temp, int count){
		if(n>0){
			moveDiscs(n-1, from, temp, to, count+1);
			System.out.println("Moved disc "+ n + " from peg "+ from+ " to peg "+to);
			moveDiscs(n-1, temp, to ,from);
		}
	}

}
